from board.board_helper import BoardHelper
from typing import Tuple, List

"""
Este método retorna el mejor valor posible para el jugador (vector en R^N) y el mejor movimiento
en formato Tuple[int, int], que son la fila y columna en que se posicionará la pieza.
"""
def max_n(board_array, player_id, depth, eval_function, player_amount) -> Tuple[List[float], Tuple[int, int]]:

    ### MODIFICAR
    
    pass